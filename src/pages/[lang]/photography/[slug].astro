---
import { languages } from '../../../i18n/content';

export function getStaticPaths() {
  return languages.map((lang) => ({ params: { lang, slug: 'bogota-between-rains' } }));
}

return Astro.redirect(`/${Astro.params.lang}/ph/${Astro.params.slug}/`);
---
